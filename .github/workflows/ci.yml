name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: hetznerreceiver/go.mod

      - name: Vet
        working-directory: hetznerreceiver
        run: go vet ./...

      - name: Run tests
        working-directory: hetznerreceiver
        run: go test -v ./...
